# Changelog

## v0.2.0

  - Added top-level commands `merge`, `intersect` and `difference`
    (see `sbwt merge|intersect|difference --help`). Each command takes two
    SBWT index files as input and produces a new SBWT index file as
    output.
